/* jshint expr: true */

function House(numBedrooms) {
  this.numBedrooms = numBedrooms;
}

        const myHouse = new House(4);

 // Checking the code to Verfiy it is instace of The Constructor Function

        console.log(myHouse instanceof House);
